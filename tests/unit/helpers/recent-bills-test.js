import { recentBills } from 'legislator-lookup/helpers/recent-bills';
import { module, test } from 'qunit';

module('Unit | Helper | recent bills');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = recentBills([42]);
  assert.ok(result);
});
